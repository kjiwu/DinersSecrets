<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorBackgroundDark</item>
        <item name="android:textColor">@color/colorTextNormal</item>
    </style>

    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="ToolBarTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="titleTextColor">@color/colorBackgroundDark</item>
        <item name="navigationIcon">@mipmap/back</item>
        <item name="android:theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
    </style>

    <style name="ToolBarTitleStyle">
        <item name="android:textColor">@color/colorBackgroundDark</item>
        <item name="android:textSize">@dimen/item_title_size</item>
    </style>

    <style name="CookbookItemTitleStyle" parent="Theme.AppCompat.Light">
        <item name="android:textSize">@dimen/item_title_size</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="SearchGroupTitleStyle">
        <item name="android:textColor">@color/colorBackgroundDark</item>
        <item name="android:textSize">@dimen/group_title_size</item>
    </style>

</resources>
